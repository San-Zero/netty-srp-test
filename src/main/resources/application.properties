# Server configuration
server.port=8080
server.host=0.0.0.0

# Client configuration
client.default.host=localhost
client.default.port=8080

# Security configuration
srp.prime.bits=2048
srp.hash.algorithm=SHA-256

# Connection settings
connection.timeout.seconds=30
session.timeout.minutes=30